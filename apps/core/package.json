{
  "name": "@summit/core",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Shared code for the Summit apps (Planner, Fitness, Eat): GitHub data layer, theme, password gate, dark mode, shell pieces. Source-only — each app's Vite build compiles it.",
  "exports": {
    ".": "./src/index.js",
    "./db": "./src/db.js"
  },
  "scripts": {
    "test": "vitest run"
  },
  "peerDependencies": {
    "lucide-react": "*",
    "react": "*",
    "react-dom": "*"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
